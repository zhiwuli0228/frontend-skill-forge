export {
  SELECTOR_REGISTRY,
  getSelector,
  listSelectors,
  type SelectorEntry,
  type SelectorPriority,
} from './selectors';

export { validateScenarioShape, type ScenarioFixture } from './fixture';

export {
  buildEvidencePath,
  buildScreenshotName,
  buildFindingMarkdown,
  type EvidenceMetadata,
} from './evidence';
